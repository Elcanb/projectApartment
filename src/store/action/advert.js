import axios from 'axios';
import { GET_ADVERT } from './types'

export const getAdvert = () => dispatch => {
    //Back end version
    // axios.get('/adverts')
    //  .then(res => dispatch({ type: GET_ADVERT, payload: res.data }))
}